VITE_API_BASE_URL=http://localhost:8000

# Optional: expose your Google OAuth client ID to the frontend
# (not required for the current server-redirect flow, but handy for UI hints)
VITE_GOOGLE_CLIENT_ID=773886975689-lc89cfh8aumg8p9rpva73ohg64n8tjeu.apps.googleusercontent.com

# Optional: override the Google auth route if your backend uses a custom path
# default is /api/auth/google
VITE_GOOGLE_AUTH_PATH=/api/auth/google

# Leave secrets (client secret) only on backend; never put them here.
